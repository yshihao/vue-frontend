import Vue from 'vue';
import Vuex from 'vuex';
import windowState from './windowState';

Vue.use(Vuex);

export default new Vuex.Store({
    state: {},
    mutations: {},
    actions: {},
    modules: {
        windowState
    }
});
